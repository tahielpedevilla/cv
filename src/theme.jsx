import {extendTheme} from "@chakra-ui/react";
const theme = extendTheme({
  styles: {
    global: {
      fonts: {
        heading: "Poppins",
        body: "Poppins",
      },
    },
  },
});

export default theme;
